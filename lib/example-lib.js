/*
  lib/ contains internal modules used by index.js.

  In Holepunch-style packages, index.js is the public API and lib/ is where
  implementation details are split by concern: encodings, protocol messages,
  stream helpers, storage adapters, state machines, or small shared utilities.

  Files in lib/ are usually not documented as public API. They can change as
  the package evolves, while index.js remains the stable user-facing surface.

  Replace this file with real internals when the package has a concrete job.
*/
